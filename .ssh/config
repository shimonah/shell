# create config file 
touch ~/.shh/config

# add this lines

Host alias-name
        HostName 127.0.0.1
        User usr-name

Host another-alias-name
        HostName 127.0.0.2
        User another-usr-name


# use ssh alias
ssh alias-name
